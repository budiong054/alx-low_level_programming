#include <stdio.h>

/**
 * main - prints the phrase "with proper grammar, but the outcome is apiece of art," followed by a new line
 * return 0 if executed successfully and a non-zero otherwise
 **/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
