#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the first integer
 * @b: the second integer
 * Return: the result
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
